# gpio-interrupt-demo

Demonstrates use of GPIO API to enable and handle interrupts generated by flipping the on-board switched.

The demo blinks LED 0, flipping a switch triggers a printout and all of the LEDs light up.
