# pmp-demo

Demonstrates use of PMP (Physical Memory Protection) API by configuring several memory ranges with different PMP settings, then
reading and writing accesses are tested to each from both machine and user modes.

The demo is only intended for targets supporting PMP and user mode.
